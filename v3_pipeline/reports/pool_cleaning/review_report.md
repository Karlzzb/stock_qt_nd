# pool_cleaning 独立复核报告（怀疑者立场）

> 复核对象：`v3_pipeline/reports/pool_cleaning/` 全部产物 + `v3_pipeline/scripts/run_pool_cleaning.py`。
> 复核人：独立复核员，2026-09-02。
> 进度流水：`review_progress.log`。

## 裁决：通过（核心数字全部独立复核成立），附 3 项文本勘误与 1 项披露缺口

所有声称的核心数字（剔除量、基线重算、80.43% 命中率、98.6% 交叉验证、测试段计数）经独立复算逐位或全样本一致。
发现的瑕疵全部位于报告文本层与披露完整性层，不影响任何结论数字与下游产物（`excluded_events_*.parquet` 可直接复用）。

## 逐项证据

### 1. 复跑与产物对拍：通过

脚本复跑耗时 25s（与声称一致），5 个产物文件与复跑前快照逐字节/逐值一致（3 个 CSV diff 为空、2 个 parquet `equals=True`）。
内嵌口径自检断言（raw val 主 0.499727/0.580019、备 0.557332/0.605003，日数/事件数）复跑时全部通过。

### 2. ST as-of 重建正确性：通过

- 全样本扫描 namechange（13799 行 / 5895 只）：同代码同 start_date 重复行 = 0，排除 as-of 歧义。
- 2082 个含 "ST" 的名称全部匹配风险警示前缀（`*ST/ST/S*ST/SST/G*ST/GST`），无嵌入式误判形态（如英文单词含 ST），"全样本无误判形态"声明成立。
- 用 pandas `merge_asof` 独立重写 as-of 打标（与脚本的 searchsorted 循环不同实现路径），对双池全部 45140 个事件逐事件比对：`f_st` 一致率 100%（主 330、备 1762 个标记全同）。
- 边界 off-by-one 双向钉死（stk_limit 精确比 up_limit/pre_close）：
  戴帽端 6 例（000048 2018-07-02、000809 2017-04-26/2022-04-29/2024-04-29、002602 2024-11-08、002689 2025-12-02）全部在新名 start 日当天从 1.10 跳到 1.05；
  摘帽端 4 例（000809 2018-06-11/2023-06-02/2025-04-25、002602 2025-11-12）全部在新名 start 日当天回到 1.10。
  "start 含当天、旧名称有效至下一 start 前一日"语义独立证实。
- "2026-07-06 主板 ST 改 ±10%"计划外发现独立证实：002586/002689（深）与 600053（沪）均在 07-03 挂 1.05、07-06 起挂 1.10，沪深同日切换。
- 111 个例外独立复算分解完全一致：8135 个 2007 后 ST 日中 8024（98.64%）在 [1.045,1.055] 带内；例外的 101 天在 2026-07-06 及以后，更早的 10 天 = 3 天复牌挂 10%（600758/000681/600727）+ 7 天 002118 超低价取整（1.0408~1.0577）。
- end_date 全局声明复核一致：7904 个非末行 end_date 均 ≠ 下一行 start_date，27 行 end 为空却有后续行，典型 gap=1 天（7607 行）。

### 3. 一字涨停判定：通过（口径一致，容差方向保守）

- 价格口径：抽样 30 个 `f_limitup` 标记事件，stk_limit 的 up_limit 与日线 open 同为未复权原始价（open/pre_close 与 up_limit/pre_close 比值逐例一致），无复权口径错位。
- 30 例中 27 例 open == up_limit 精确相等，3 例 open 低于 up_limit 恰好 1 分（601010/600246/600481）——容差 0.01 元把"开盘低涨停 1 档"也标记为不可买，严格说非一字涨停，但已在脚本 docstring 披露且方向保守，可接受。
- 2007 前处理：d1 < 2007-01-04 的事件 `limitup_evaluable=False`、不标记、单独计数，无静默当作"非涨停"。
  计数核实：labeled 口径 train 主 125 / 备 3591、pre2001 主 78 / 备 426，与报告一致。

### 4. 停牌判定口径：通过

- 判定 = d1 无本地行情行 或 vol≤0/NaN 或 open NaN/≤0（缺行与零成交都覆盖）。
- 双向抽样各 40 例：被标记事件全部确无 d1 行情行（样本中 vol0/open_bad 分支未触发）；未被标记事件 40/40 均有有效行情行（vol>0），无误判。
- 标签机器顺延核查复跑日志确认：主 20、备 184 个个股次 bar 晚于交易所历 d1 的事件 100% 被 f_suspend 覆盖。
- 方向评估：缺行既可能是停牌也可能是本地数据空洞，误杀方向为保守（多剔），可接受。

### 5. 基线重算：通过（全表 20 行独立复算一致）

- 完全独立路径：从 feature_matrix 键 + divergence_lab events/labels 原始文件出发（不复用脚本中间结果），段划分自行实现（含双隔离带），再与脚本产物 parquet 的 seg 列断言全同后方计算。
- 标签链抽查：labels.parquet 无事件键、纯位置对齐（前 len(events) 行且 group=="div"），抽 12 个事件用 talib ATR(14) 按定义（T+1 开盘入场、high[T+1..T+20] 触及 open+2×ATR）手工重算，12/12 与存储标签一致；sig_idx 与事件日在当前日线中的位置逐例相符。
- 全表比对：event_weighted_hit 最大偏差 3.0e-8、day_weighted_hit 最大偏差 4.2e-8（浮点噪声），n_events/n_days 一致（差异仅为 NaN-hit 行计数口径，主 2 / 备 287 个 matched-but-hit-NaN 事件，多为数据末端窗口不完整，脚本按 hit.notna() 剔除，口径自洽）。
- 四个头条数字逐位核实：主 val raw 49.9727% → 组合 50.3287%（+0.36pp）；备 val 55.7332% → 55.6050%（−0.13pp）；事件加权 58.0019%→58.3924%、60.5003%→60.6765%。
- 日加权口径确认 = 逐日池命中率的信号日等权平均（零信息分数下 top3 期望即当日池均值，口径文档自洽）。
- 剔除集命中率（表 3.2）全表复核一致，含备池 val 一字涨停剔除集 80.43%（n=46）。
- 剔除量（表 3.1）全表复核一致：主 train/val 98/182（ST 79/171、停牌 9/8、涨停 12/11、重叠 2/8）；备 1290/365（ST 991/312、停牌 153/26、涨停 196/46、重叠 50/19）。

### 6. 测试段纪律：通过

`test_segment_filter_counts.csv` 与报告 §5 只含过滤计数，无任何命中率/收益统计。
测试段计数复核一致：主 82/1792、备 460/8851（另备 41 事件 d1 超数据末端，主 0）。

### 7. 报告措辞：核心结论有数字支持，但有 3 项勘误 + 1 项披露缺口

**勘误 1（事实错误，量级差 8 倍）**：§2 "合计约 9.7 万个 ST 交易日"错误。
`st_crossval_evidence.csv` 的 st_days 合计实为 11725（其 2007 后子集 8135 与 98.6% 的分母自洽）。
应为"约 1.2 万个"。

**勘误 2（以偏概全）**：§6 "剔除量占组合过滤的 94%（验证段主 171/182、备 312/365）"。
94% 仅对主池成立（171/182=94.0%）；备池为 85.5%（312/365），双池合并 88.3%。
括号内两个数字只有一个支持 94%。

**勘误 3（表述不完整）**：§1.3/§3.1 "此前事件不做此过滤、单独计数……训练段主 125 / 备 3591 事件受影响，验证段为 0"。
"验证段为 0" 只对 pre-2007 这一不可评估原因成立；实际上 `limitup_evaluable=False` 还有两类未披露原因（见披露缺口），验证段实际有主 18 / 备 62 个不可评估事件。

**披露缺口（对测试段计数有实际影响）**：stk_limit 数据存在两个静默覆盖空洞，报告与 `test_segment_filter_counts.csv` 均未披露——
(a) 北交所（920/832 前缀）从无 stk_limit 覆盖：labeled 口径 train 主 14 / 备 65、val 主 18 / 备 62 个事件不可评估；
(b) 646 个 stk_limit 日文件被截断在整 5800 行（自 2021-12-29 起），2025-2026 年 SH 603/605/688 股票大量缺行，致测试段主 412 / 备 1757 个事件不可评估（含非北交所主板股）。
后果：测试段 `f_limitup` 计数（主 11 / 备 41）是**下界**，终审直接复用时需知悉。
对 train/val 基线的实质性影响已量化排除：全量扫描 train/val 不可评估事件的 d1 行情，无一例真正的"涨停价封死开盘"漏判（北交所近限事件均为无涨跌幅限制的上市首日；001914.SZ 13 个事件 open/pre_close 均远离涨停）。

**措辞核查（无问题项）**：
- "清洗让基线移动很小（≤0.4pp）"——验证段 +0.36pp/−0.13pp 支持；训练段 −1.50pp/−1.33pp 在同一条目内如实披露，未隐匿。
- "主池验证段 +0.34pp 全来自 ST"——剔 ST 50.31% vs 原始 49.97% = +0.34pp，组合 +0.36pp，"全来自"近似成立（差 0.02pp）。
- "2026-07-06 后 ±5% 近似判涨停逻辑失效"风险提示审慎且正确。
- 测试段仅计数的纪律声明与实际产物一致。

## 结论

核心数字与方法论（ST as-of 重建、一字涨停口径、停牌口径、基线重算、测试段纪律）全部独立复核成立，产物可复现、可供下游复用。
建议修订报告文本：§2 的"9.7 万"改为 11725（约 1.2 万）、§6 的 94% 限定为主池口径、§1.3/§3.1 补充北交所无覆盖与 5800 行截断两类不可评估原因，并在 §5 注明测试段 f_limitup 计数为下界。
上述修订均为文本层，无需重跑任何计算。
