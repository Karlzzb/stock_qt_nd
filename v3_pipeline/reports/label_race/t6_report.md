# T6 标签赛全量裁决报告（issue #26）

日期：2026-09-03
产出：v3_pipeline/reports/label_race/（19×2 张配置为行的指标表、当选配置汇总、裁决 JSON、折外概率产物、台账、心跳日志）
管线库：v3_pipeline/src/label_race.py（复用 train_eval_pipeline.py 与 label_candidates.py）；驱动：v3_pipeline/scripts/build_race_labels.py、run_label_race.py
单元测试：v3_pipeline/tests/test_label_race.py（17 例，合成数据已知值断言，无外部磁盘依赖）
预登记：issue #26 置顶评论（2026-09-03 05:10:15 UTC 落盘，早于最早指标产物 523 秒，独立复核已核时戳）

## 结论

**当选标签：hit_N20_k2.0（背离狙击命中标签：T+1 开盘入场，20 交易日内触及 +2 倍 ATR(14)）。**
正赛（主备合并池）val 段：头部五名精确率（日加权）0.5750，十九候选第一，领先次席 open_exec_pos_10d（0.4906）8.4 个百分点；平均精确率 0.6225 ≥ 十九候选中位数 0.5794，中位数约束通过。
对照（主池）同网格同口径独立跑赛，同标签当选（头部五名精确率 0.5165，平均精确率 0.6283 ≥ 中位数 0.5599），方向跨池一致。
裁决经独立复核（证伪式）全部 PASS 后放行，本报告落盘即锁定，候选标签集与当选结果不再变更（#20"测试段每个候选只碰一次"）。

## 验收断言（issue #26 AC）

1. **十九个候选全部出数，指标表以配置为行落盘**：PASS。
   合并池与主池各 19 张 metrics_{pool}_{candidate}.csv，每表恰 36 行（配置为行），覆盖预登记 36 组网格无重复无缺失（独立复核逐行核对）。
2. **裁决按预登记规则执行，无自由裁量**：PASS。
   选配置与裁决规则均为代码化纯函数（label_race.select_best_config / adjudicate），预登记评论先于任何结果；独立复核自写规则重算，38 个当选 config_id 与两处裁决结论逐字段一致。
3. **当选标签独立复核后落盘 #20**：PASS。
   复核记录 t6_review_record.md：裁决重算、独立重跑抽验（逐位一致）、标签抽验、纪律核查、复现产物核查五项全 PASS；#20 锁定评论随本报告落盘。

## 样本量兜底规则触发（#20 预登记）

主池训练段事件数实测 2838 < 3000，兜底规则触发：主备合并池升为正赛，主池降为对照。
合并池构造：master_main ∪ master_backup 行并集不去重（两池同 (ts_code,date) 事件仅 38 对，背离几何不同各留一行），event_id 加池前缀，pool 溯源列为字符串（数值口径自动排除，不进特征）。
dtype 归一登记：downtrend/hammer_signal 在备池为 object(bool, 含 None)、主池为 int8（T4 构建期漂移），合并前统一为可空 Int8，两池特征列口径一致（2060 列）。
合并池段计数：train 22963（≥3000 满足）/ val 10231 / test 10643（零触碰）；段界与两段 30 个交易日隔离带断言通过。
兜底触发条件由驱动实测断言守护（主池 <3000 且合并池 ≥3000 才允许以正赛身份跑 merged），非硬编码。

## 口径登记（全部预登记，先于结果）

- 候选十九个：狙击标签 hit_N20_k2.0（各池 labels.parquet div 组逐位对齐装载，合并池每行携带本池版本）；cur 族 `cur_pos_{h}d` = 1{close[T+1+h]/close[T]−1>0}；open_exec 族 `open_exec_pos_{h}d` = 1{close[T+1+h]/open[T+1]−1>0}；h ∈ {3,5,10,15,20,25,30,45,60}。
  视野语义登记：出场统一为 T+1+h 收盘（复用已通过因果性单测的 label_candidates 算子），故 cur 族持有 h+1 个交易日、open_exec 族自 T+1 开盘持有 h 个交易日收盘；两族出场日对齐，比较口径公平。
- 收益按该票第 h+1 个后续可用 bar 取值，尾部截断/停牌顺延产出 NaN；NaN 保留、按段剔除并只对 train/val 登记计数（主池标签 NaN 至多 11 行，全在尾部视野）。
  标签构建因果性断言：抽样 20 只（实测 19 只）截头部 200 行重算逐位一致；独立复核手工抽验 8 事件逐位一致、全部 NaN 行逐一证实为尾部 bar 不足。
- 网格：num_leaves{15,31,63} × min_data_in_leaf{50,100,200} × learning_rate{0.05,0.10} × feature_fraction{0.6,0.8} = 36 组全因子（≤50）；其余参数沿用 #25 冒烟口径（bagging 0.8/5、复现性四件套、num_threads=8、上限 1000 轮早停 50）。
- 每配置管线与 #25 一致：训练段五折时间序列折外概率 → 校准层 [p, p²] → 终模（轮数=五折 best_iteration 均值）→ train_oof/val 双段指标。
- 选配置：val 头部五名精确率（日加权）最高 → 平局 val 平均精确率较高 → 再平局网格序靠前。
- 裁决：十九候选各取当选配置后 val 头部五名精确率（日加权）最高者当选（平局同上序），且其 val 平均精确率 ≥ 十九候选中位数，否则无当选。

## 正赛（主备合并池）十九候选当选配置汇总

| 候选标签 | 中文全名 | 配置 | val 基率 | val 平均精确率 | val 头部五名精确率（日加权） |
|---|---|---|---|---|---|
| **hit_N20_k2.0** | **背离狙击命中标签（T+1 开盘，20 日内 +2×ATR(14)）** | L31/m50/η.05/f.8 | 0.5973 | **0.6225** | **0.5750** |
| cur_pos_3d | T 收盘入场收益大于零（3 日视野） | L63/m50/η.05/f.8 | 0.5025 | 0.5737 | 0.4673 |
| cur_pos_5d | 同族 5 日视野 | L63/m200/η.05/f.6 | 0.5238 | 0.6088 | 0.4807 |
| cur_pos_10d | 同族 10 日视野 | L15/m50/η.10/f.8 | 0.5465 | 0.5862 | 0.4878 |
| cur_pos_15d | 同族 15 日视野 | L31/m100/η.10/f.8 | 0.5351 | 0.6045 | 0.4834 |
| cur_pos_20d | 同族 20 日视野 | L63/m200/η.05/f.6 | 0.5491 | 0.6133 | 0.4770 |
| cur_pos_25d | 同族 25 日视野 | L15/m100/η.10/f.8 | 0.5370 | 0.5794 | 0.4716 |
| cur_pos_30d | 同族 30 日视野 | L15/m50/η.05/f.8 | 0.5336 | 0.5716 | 0.4810 |
| cur_pos_45d | 同族 45 日视野 | L15/m50/η.05/f.8 | 0.5177 | 0.5036 | 0.4841 |
| cur_pos_60d | 同族 60 日视野 | L31/m100/η.05/f.6 | 0.5102 | 0.5436 | 0.4793 |
| open_exec_pos_3d | T+1 开盘入场收益大于零（3 日视野） | L31/m50/η.05/f.6 | 0.5068 | 0.5793 | 0.4711 |
| open_exec_pos_5d | 同族 5 日视野 | L31/m50/η.05/f.8 | 0.5274 | 0.6206 | 0.4803 |
| open_exec_pos_10d | 同族 10 日视野 | L63/m50/η.10/f.8 | 0.5493 | 0.5789 | 0.4906 |
| open_exec_pos_15d | 同族 15 日视野 | L15/m100/η.10/f.8 | 0.5358 | 0.5924 | 0.4802 |
| open_exec_pos_20d | 同族 20 日视野 | L31/m200/η.05/f.6 | 0.5484 | 0.5844 | 0.4745 |
| open_exec_pos_25d | 同族 25 日视野 | L31/m50/η.10/f.6 | 0.5372 | 0.5739 | 0.4780 |
| open_exec_pos_30d | 同族 30 日视野 | L63/m50/η.10/f.6 | 0.5341 | 0.5996 | 0.4849 |
| open_exec_pos_45d | 同族 45 日视野 | L31/m50/η.05/f.8 | 0.5175 | 0.5263 | 0.4864 |
| open_exec_pos_60d | 同族 60 日视野 | L31/m50/η.05/f.8 | 0.5097 | 0.5089 | 0.4759 |

注：配置列 L=叶子数/m=叶子最小样本数/η=学习率/f=特征采样比例；完整 36 配置指标见各 metrics_merged_*.csv。
val 平均精确率中位数 0.5794；狙击标签 0.6225 过约束。

## 对照（主池）裁决参考

同网格同口径：hit_N20_k2.0 当选（头部五名精确率 0.5165、平均精确率 0.6283 ≥ 中位数 0.5599），配置 L15/m50/η.05/f.6。
收益族头部五名精确率全线 0.41~0.45，与正赛同序（狙击标签显著领先）。
对照结论与正赛一致，当选结果对池口径稳健。

## 复现性与纪律断言台账

- 段界与隔离带：两池 assert_segment_integrity 四项硬断言通过（合并池段计数 22963/10231/10643）。
- 泄漏排除：特征列 2060，feature_master 排除模式零命中。
- 复现性：两池 38 个当选配置 OOF 复现断言全部逐位一致（np.array_equal equal_nan）。
  模式登记：首批跑赛产物早于折外概率落盘加固，当选配置以"全程自复跑两遍逐位一致后补档"（self_rerun_x2，即 #25 口径）完成断言并补落 oof_{pool}_{candidate}.parquet；此后断点续跑一律走"落盘产物对重算"（stored_vs_rerun）。
- test 段零触碰：仅在场断言（合并池 10643 行），全产物无任何 test 段指标或逐行统计（独立复核 grep 全量产物核实）。
- 心跳与断点续跑：progress.log 全程阶段心跳；合并池正赛 5369s（3 worker），主池对照 636s。

## 评审登记（两轴代码评审后）

- 评审修复一（Spec 轴实锤）：复现断言原实现为"两次新跑互证"，未与被裁决跑次的折外概率比对，断点续跑的陈旧指标表可蒙混；已加固为跑赛全程落盘 36 配置折外概率、断言以落盘产物为对照基准（首批产物按上节模式补档）。
- 评审修复二（Spec 轴）：兜底触发条件原为文档硬编码，已改为驱动实测断言（主池 <3000 且合并池 ≥3000）；对照池补跑同款复现断言；CLI 移除未登记的 --pool backup 选项。
- 评审修复三（Standards 轴）：术语口径收编（"p@5"→头部五名精确率/平均精确率全称），GRID 推导式循环变量更名避免遮蔽模块别名，日志配置数去硬编码，候选标签中文全名列入汇总表（命名纪律）。
- 评审登记不改（Spec 轴询问项）：cur 族视野语义为 T 收盘→T+1+h 收盘（持有 h+1 日），系预登记评论明确定义且复用已验证算子，两族出场日对齐，比较公平；记此备查。
- 标签表较主表多 30 行指数伪股事件（000001.SH/399001.SZ 等），系 T2 主表既定剔除，左连后不进模型；主表行标签零缺失。
- 校准层 OOF/终模分布错位与 train_oof 行 in-sample 乐观为 #25 已登记固有限度，本赛裁决只认 val 口径。

## 独立复核结论（证伪式，t6_review_record.md）

五项全 PASS：裁决重算（自写规则，38 个当选 config 与两处裁决逐字段一致）；独立重跑抽验（当选标签当选配置全流程重算，19 个指标字段与指标表逐位一致，容差 0）；标签抽验（8 事件手工重算逐位一致，NaN 全部证实为尾部截断）；纪律核查（test 零触碰、预登记时戳先于结果、段计数与特征口径复核通过）；复现产物核查（38 个折外概率文件在场，抽 2 候选重算逐位一致）。
复核意见：放行，当选标签可锁定。

## 对后续票据的约束（#27 特征精选）

- 特征精选的唯一标签 = hit_N20_k2.0，特征池 = 主表 2060 列，universe = 主备合并池（正赛口径），主池留作对照。
- 当选标签的测试段首碰留给终审；特征精选一切选择动作只在验证段。
- 训练评测口径沿用 #25/#26 管线（五折 OOF + [p, p²] 校准层 + 头部五名精确率日加权主口径），不得另起口径。
