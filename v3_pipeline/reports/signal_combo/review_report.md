# 独立复核报告: 信号组合探索(issue #11, signal_combo)

复核人立场: 怀疑者。
复核对象: `v3_pipeline/reports/signal_combo/` 全部产物 + `v3_pipeline/scripts/run_signal_combo.py`。
复核日期: 2026-09-02。
复核方法: 原始产物先整树备份至 `/tmp/review_backup/signal_combo_orig`, 再原地复跑对拍; 池构建/剔除标记/指标/bootstrap 全部用独立编写的代码重算(仅复用已验收的 run_pool_cleaning 原语与已逐位对拍过的落盘标签)。

## 裁决: 通过(附 4 条注记, 其中 2 条建议整改措辞/披露)

执行方三项核心声称(四池全未过活线、口径自检、测试段零出行)全部成立。
四池"未过活线"的数值结论稳健, 主池守擂成立。

## 必查项逐项证据

### 1. 复跑确定性 — 通过

原地复跑 `run_signal_combo.py`, 耗时 31s(与声称一致), 退出码 0。
`results_signal_combo.json`、`pools/{P1..P4}/{events,c1}.parquet`、`backup_events.parquet` 与备份原件 md5 逐位一致。
`signal_combo_report.md` 唯一差异为生成时间戳行(18:34→18:38 UTC)。
`progress.log` 去时间戳后内容逐行一致。
复跑中脚本内全部 assert(备池事件对拍 37012、备池标签与 w_zigzag_p05_s5 逐位一致、baseline_recalc 20 行复现、主/备落盘标记共享键一致)再次通过。

### 2. P1 对齐口径 — 通过(附注记 A/B)

独立用交易所历(rpc.load_calendar, 与执行方用的 divergence_lab all_dates 不是同一日历来源)重建 ±1 交易日聚类: 60 事件, 与执行方产物集合相等。
匹配规则审查: 代表日=簇内最早主池事件日, 标签取该主池事件行(hit_N20_k2.0=T+1 开盘入、20 根内 high 触及 entry+2×ATR(14,T)), 标签本身无前视。
同股同日去重口径(P2 取主池行)经核验无实质影响: 25 个同 (s,d) 共享键上主/备标签、mfe、atr、entry 全部一致(0 冲突)。
60 这个数独立重算一致: 60 = 60 簇(30 raw train + 29 raw val + 1 pre-2001)。
簇成分(独立统计): 25 簇含同日备池成员、26 簇备池成员仅早 1 日、9 簇含晚 1 日(+1)的备池成员。

注记 A(+1 日前视性): 9/60 簇的"共振确认"含信号日之后 1 个交易日的 zigzag 事件, 该确认在 T+1 开盘入场时点不可知。
这是预登记 ±1 口径的固有属性(脚本 docstring 与任务背景一致; issue #11 正文无更细口径存档), 非执行偏差。
且该偏差只可能抬高 P1 表观命中, P1 最终未过活线, 结论方向保守, 不动摇判定。

注记 B("38 对"口径): 报告 §3 "全史同股同日命中仅 38 对" 经溯源 = m_fractal15_full 全史事件(含封存测试段, max 2026-05-26) ∩ m_zigzag05_nofilter 全史 37012 事件的同 (s,d) 计数。
测试前口径下该数字为 25(主池 6366 ∩ 备池 28161)。
该句仅作结构性叙事(不含任何结局/收益信息), 不影响任何指标与判定; 但与报告"零测试段行"的自律存在字面张力, 建议改为测试前口径的 25 或注明口径。

### 3. P3/P4 成分 — 通过

GRID9 = g_o{13,15,17}_d{06,08,10} 与 `signal_param_grid_report.md` §1 "主轴家族密度(9 配置)" 明列的 9 配置完全一致; 副轴 sa_/sb_ 4 配置未混入。
独立重建: P3 并集 22712 事件、P4 ≥2 共识 18890 事件, 与执行方产物集合相等; 网格 9 配置原始事件合计 57369 与日志一致。
P4 共识计数口径 = 簇内不同配置名集合大小 ≥2, 与预登记描述一致; 代表日=簇内配置数最多日(平手取最早)经代码审查与独立复现一致。
跨配置同 (s,d) 标签一致性: 34657 个共享键 0 冲突, 故 P4 代表配置选择对标签无实质影响。

### 4. P2/P3 指标与 bootstrap — 通过

指标: 用独立代码(自建池 + 自建剔除标记 + 落盘标签)重算全部 6 池 × train/val 的清洗后事件数与事件加权命中率, 20/20 格与 `results_signal_combo.json` 逐位一致。
含必查的 P2 val 0.6102(n=10329)、P3 val 0.6020(n=11641)、主池 val 0.6050(n=3259), Δ(P2−main)=+0.0053、Δ(P3−main)=−0.0030 与报告一致。
bootstrap 实现审查(`boot_compare`/`_day_cells`): 按日聚类(日→簇内和/计数)后对日有放回抽样, 事件加权均值=和/计数之比, 双段联合=同一复制序号下 train/val 均 >0 的比例取补, 与预登记"非双段同向为正的比例"一致, 实现正确。
独立重写 bootstrap(异种子 seed=123/7, B=10000)复核全部 12 组对比: Δtrain/Δval 全部逐位一致; p_joint 最大偏差 0.021(P2 vs backup 命中率 0.6015 vs 0.5804), 在 B=10000 蒙特卡洛误差(2×SE≈0.01, 8 组取最大偏差属正常涨落)之内, 无任何一组接近 0.05 活线。

### 5. 剔除标记并集口径 — 通过

need 键 50772、落盘未覆盖 16300, 与执行方日志一致。
用 rpc 原语 + 自写胶水独立现算缺失键: ST=616 / 停牌=97 / 涨停=49, 与执行方完全一致。
逐键核对: P2 34502 键 0 误配(f_any 合计 1997), P3 22712 键 0 误配(合计 1011)。
成员 OR 并集核对(自建簇成员表): P1 0 误配(3/60), P4 0 误配(900/18890)。
P2 并集算术: 6366 + 28161 − 25(同 (s,d) 重合) = 34502, 成立; 剔除并集算术 294 + 1704 − 1 = 1997, 成立。
现算机器与 `run_pool_cleaning.py` 原机逐行比对: f_suspend/f_limitup 公式、LIMIT_DATA_START、LIMIT_TOL 一致; `attach_d1_bars_tolerant` 仅增加 volume 列名兼容与跳过 own_next 核查(该核查在原机仅用于日志, 不影响标记), 无口径偏移。
主/备落盘标记共享 38 键 f_any 全一致(脚本 assert 复跑通过)。

### 6. 结论措辞 — 基本通过(附整改建议 C)

报告 §3 以"稀缺性-命中率权衡"为题, 给出 P1 n=29/28、对主池 Δ 的 95%CI 宽达 [−16.3, +22.1]pp、以及日级共振稀缺的结构性注记; §4 CI 列显示 P2/P3/P4 的 val CI 均在 ±6pp 以内。
即"样本量不足(P1)"与"无增益(P2/P3/P4)"的素材区分在报告内齐备, 执行方核心声称亦将 P1 单列为"结构性稀缺…一切差异皆噪声"。
整改建议 C: §5 判定节与 progress.log 将四池并列"未过活线/P1=死", 未在判定处复述 P1 属功效不足不可判死; 建议 §5 P1 条补一句"未过活线且因样本量不足(n≈30/段)不可判死, 与 P2/P3/P4 的实锤无增益性质不同"。

### 7. 测试段纪律 — 通过(附注记 B)

全部产物(pools P1-P4 events/c1、backup_events)最大日期 ≤2022-10-31; 输入侧网格 events.parquet 亦已预过滤(max 2022-10-31)。
`results_signal_combo.json` 段键仅 train/val, verdict 无测试段信息。
唯一涉测试段的痕迹为注记 B 的"38 对"计数(源自全史落盘文件, 不含结局信息)。

## 补充核验(超出必查项)

备池标签独立抽查: 自写 Wilder ATR(14) 与 hit 判定, 随机 30 事件 0 误配。
报告 §1/§2 表与 JSON 抽查(P3 val 62.7 事件/周、31.1%、60.2/50.4/+9.8; 主池 val 17.6/60.1/51.1; 备池 56.8)一致。
任务背景所引清洗后基线(主池 val 日加权 50.33%、备池 55.61%)与 `baseline_recalc.csv` excl_combined 行一致(0.503287/0.556050)。
口径披露注记 D(不影响结论): 本报告主表用网格口径的简单切分(无 embargo), 主池 val 清洗后 3259 事件; pool_cleaning 基线为 embargo 口径(剔除 2022-09-13~2022-10-31 与 2018-11-19~2018-12-28 事件), 2961 事件。
两口径并存已经"embargo 口径复现 baseline_recalc 全 20 行"自检桥接, 且与已验收的 signal_param_grid 主口径一致, 池间比较内部自洽; 建议报告显式一句对账, 免读者误比。

## 产物清单(复核相关)

复核进度日志: `v3_pipeline/reports/signal_combo/review_progress.log`。
原始产物备份: `/tmp/review_backup/signal_combo_orig/`(复跑对拍用, 可按需清理)。
