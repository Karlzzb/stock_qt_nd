# V3 Pipeline 失败总结与建议

**日期**: 2026-08-28
**状态**: V3 已在 Stage 4 测试集评估失败（年化 -71%，NO-GO）

---

## 根本原因（双重失败）

### 问题 1：数据质量 - 收益率截断
- **事实**: feature_cache_v3.parquet 所有收益列被截断到 +15%
- **源头**: `src/feature_pipeline_v2.py:401` 的止盈逻辑（EXPECTED_PROFIT = 1.15）
- **影响**: 
  - 训练数据分布与真实交易不一致
  - Stage 1-2 的 Rank IC/ICIR 虚高（IC=0.98, ICIR=44.2）
  - 模型学不到真实收益的尾部分布

### 问题 2：特征集边缘不足
- **事实**: 即使用截断数据，Stage 3 回测显示 3d horizon 年化 -57.55%
- **原因**: 
  - 2022-2025 验证期熊市，top-ranked 和 bottom-ranked 都亏损
  - Top vs bottom 相对优势只有 +2.05%/期
  - 交易成本 0.13%/trade 吞噬微弱边缘
- **结论**: 当前 631 特征在该时期没有可盈利的信号

---

## 为什么 Stage 1-2 看起来成功？

**Rank IC/ICIR 不能预测交易盈利性**

| Stage | 指标 | 值 | 含义 |
|-------|------|-----|------|
| Stage 1 | Rank IC | 0.98 | 能准确排序（相对） |
| Stage 1 | ICIR | 44.2 | 排序非常稳定 |
| Stage 3 | 年化收益 | -57.55% | 不能赚钱（绝对） |
| Stage 4 | 测试集年化 | -71.05% | 测试集更差 |

**原因**: 在熊市里，正确排序"亏5%的股票 > 亏10%的股票"可以达到 IC=0.98，但两者都亏钱。

---

## 已验证的事实

1. ✅ 数据被截断：30d horizon 有 31.7% 样本 = 0.15
2. ✅ Stage 3 官方回测：3d horizon = -57.55% 年化（验证集 2022-2025）
3. ✅ Stage 4 测试集：3d horizon = -71.05% 年化（测试集 2025-08-2026-08）
4. ✅ 所有 horizon 都只测试了 3d（其他 6 个 horizon 没有 Stage 3-4 数据）
5. ✅ V2 实盘：+12% 年化（2026-01-26→05-07），虽然远低于回测 +41%，但至少盈利

---

## 下一步行动（按优先级）

### 选项 A：放弃 V3，修复 V2（推荐）
**理由**:
- V2 至少实盘盈利（+12% 年化）
- V3 特征集在 2022-2026 完全失效
- V3 的 3,865 行代码可复用（回测框架、评估工具）

**行动**:
1. 回到 Issue #1：修复 V2 的回测 vs 实盘 gap（+41% → +12%）
2. 找出 V2 回测的数据泄漏或过拟合问题
3. 用 V3 的回测框架验证修复后的 V2

**预期**: 2-3 天

---

### 选项 B：修复 V3 数据并重做（中等风险）
**前提**: 相信特征集本身OK，只是数据问题

**步骤**:
1. **修改 V2 特征管线**（2 小时）
   - 在 `src/feature_pipeline_v2.py` 添加 `raw_future_return_*d` 列（无截断）
   - 保留 `future_return_*d`（截断版，V2 用）

2. **重新生成 feature_cache_all.parquet**（2-3 小时）
   - 1.4M 行 × 7 horizons × 2 版本收益列

3. **重做 V3 Stage 1-4**（4-6 小时）
   - 用 `raw_future_return_*d` 重新训练 7 个 horizon
   - Stage 1 用**回测收益**选horizon（不是 IC/ICIR）
   - Stage 2-4 照常

**风险**: 如果重做后还是负收益，说明特征集本身不行，白费 1 天

---

### 选项 C：彻底重新设计 V3（高成本）
**理由**: 当前特征集和 label 设计都有问题

**需要改动**:
1. 重新设计 label（不是简单的百分位 rank）
2. 特征工程（当前 631 特征不够）
3. 不同的市场环境（2022-2025 可能本就不可交易）
4. Long-short 策略（对冲市场风险）

**成本**: 1-2 周

---

## 我的建议

**立即执行选项 A**：
1. 放弃 V3 当前实现
2. 回到 V2，用 V3 的回测框架诊断 V2 的回测 vs 实盘 gap
3. 如果 V2 修复成功且稳定盈利，V3 就没有必要了
4. 如果未来要重启 V3，至少知道了两个必须避免的坑：
   - ✗ 不要用 IC/ICIR 作为 Stage 1 选择标准
   - ✗ 不要截断收益率数据

**时间轴**:
- 今天：写 memory 记录 V3 失败教训
- 明天：开始修复 V2（Issue #1）

---

## 需要写入 memory 的教训

```markdown
---
name: v3-failure-postmortem
description: V3 ranking pipeline 失败的根本原因与教训（2026-08-28）
metadata:
  type: project
---

V3 在 Stage 4 测试集彻底失败（年化 -71%）。根本原因有二：

**1. 数据截断（+15% cap）导致指标虚高**
- feature_cache_v3.parquet 收益被 `src/feature_pipeline_v2.py:401` 截断
- Rank IC/ICIR 虚高（IC=0.98, ICIR=44.2）但不能预测真实盈利
- **教训**: 训练数据必须和交易数据分布一致；IC/ICIR 只能衡量排序能力，不能衡量盈利能力

**2. 特征集在 2022-2026 无盈利边缘**
- 即使用截断数据，Stage 3 回测也是 -57.55% 年化
- Top vs bottom 相对优势仅 +2.05%，不足以覆盖交易成本
- **教训**: Stage 1 必须用**实际回测收益**选 horizon，不能只看排名指标

**Why**: V3 试图用 ranking objective 解决 V2 的 binary label 问题，但引入了新问题（指标与目标不一致）。

**How to apply**:
- 任何新策略的 Stage 1 评估必须包含简化回测（不能只看 IC/ICIR）
- 特征缓存生成时禁止截断收益率（除非回测也用相同截断）
- 在熊市期（2022-2025）训练的模型可能本质上不可盈利，需要多市场环境验证

相关: [[eval-protocol-v2-fast-loop]], [[rank-metrics-revelation]]
```

---

**决定**: 我现在写这条 memory，然后开始准备修复 V2。
